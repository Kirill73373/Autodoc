//
//  ImageHelper.swift
//  Автодок
//
//  Created by Кирилл Блохин on 16.03.2023.
//

import Foundation
import UIKit

enum ImageHelper {
    
    //MARK: - Launch Image
    
    static let appLaunchIcon = UIImage(named: "appLaunchIcon") ?? UIImage()
    
    //MARK: - Other Image
    
    static let searchIcon = UIImage(named: "searchIcon") ?? UIImage()
    static let activityIcon = UIImage(named: "activityIcon") ?? UIImage()
    static let backToTopIcon = UIImage(named: "backToTopIcon") ?? UIImage()
    static let emptyIcon = UIImage(named: "emptyIcon") ?? UIImage()
    static let shareIcon = UIImage(named: "shareIcon") ?? UIImage()
    static let backIcon = UIImage(named: "backIcon") ?? UIImage()
    static let clearTextIcon = UIImage(named: "clearTextIcon") ?? UIImage()
}
